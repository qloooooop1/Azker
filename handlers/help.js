module.exports = (bot) => {
    return (msg) => {
        const helpMsg = `*مركز المساعدة*\n\n` +
            `*أوامر المشرفين:*\n` +
            `/enable - تفعيل البوت في المجموعة\n` +
            `/disable - إوقف البوت مؤقتاً\n` +
            `/status - عرض حالة البوت\n` +
            `/help - هذه الرسالة\n\n` +
            `*معلومات:*\n` +
            `📌 البوت ينشر الأذكار تلقائياً\n` +
            `📌 الجدولة والإعدادات مركزية\n` +
            `📌 يدعم النصوص والملفات الصوتية والصور\n` +
            `📌 إحصائيات مفصلة للمطور\n\n` +
            `*لوحة التحكم:*\n` +
            `يتم التحكم الكامل من لوحة تحكم المطور`;

        bot.sendMessage(msg.chat.id, helpMsg, { parse_mode: 'Markdown' });
    };
};